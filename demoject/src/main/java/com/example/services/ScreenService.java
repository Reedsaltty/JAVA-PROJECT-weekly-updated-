package com.example.services;
import com.example.models.Screen;
public class ScreenService {
    // addScreen(), getAllScreens(). When a screen is added, it calls SeatService.initializeSeats() to bulk-create all seat rowsgit 

    
}